# Tests Directory

検定スクリプトをカテゴリー別にまとめています。

- `assumption`: モデルの前提条件を検証する検定
- `difference`: 母集団の差を調べる検定
- `association`: 関連性や独立性を調べる検定
- `goodness_of_fit`: 分布への適合度を調べる検定

`run_all_tests.py` を実行すると各サブディレクトリのスクリプトが順に実行されます。
